node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Local-only dev notes (placeholder secret codes etc.) — never commit
DEV-NOTES.md
.vercel

# Invitation cards contain per-friend secret codes — never commit (repo is public)
invites/

# Local DB snapshot (real player data) pulled by scripts/pull-snapshot.mjs — never commit
src/data/snapshot.local.json
*.local.json
